interface:
  display_name: "Competitor Scan"
  short_description: "Scan competitor growth signals and write a leadership-ready Chinese brief."
  default_prompt: "Use $competitor-scan to generate a business-ready Chinese competitor user-growth brief for a platform strategy lead."

policy:
  allow_implicit_invocation: true
